# 若木云卡生产环境 Nginx 总入口示例。
# 当前 docker-compose.prod.yml 默认用独立端口暴露 user/admin/api。
# 生产推荐在外层网关按域名转发：
# - shop.example.com  -> user:80
# - admin.example.com -> admin:80
# - 两个前端域名的 /api/ 与 /uploads/ 都转发到 api:8080
# 具体单服务配置见 user.prod.conf / admin.prod.conf。
